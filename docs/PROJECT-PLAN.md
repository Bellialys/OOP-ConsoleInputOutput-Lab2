# План проєкту

## Статус реалізації

Усі етапи 1–5 реалізовані. Застосунок має готове головне меню, чотири робочі модулі та автоматичні smoke-тести. Етап 6 — фінальна перевірка перед здачею — виконується зараз.

## 1. Мета

Створити один консольний застосунок C#, який об’єднує чотири навчальні завдання. Користувач запускає програму, бачить заставку, переходить до головного меню, обирає потрібний модуль, виконує його та повертається до меню.

Принцип проєкту: **простий код, зрозуміла структура, коректні алгоритми, охайна взаємодія з користувачем**.

## 2. Технічні рішення

- Мова: C#.
- Тип: Console Application.
- Цільовий фреймворк: `net8.0`.
- IDE: Visual Studio 2022 (17.8+) або VS Code.
- Сторонні NuGet-пакети: не потрібні.
- Кодування консолі: UTF-8.
- Основна гілка Git: `main`.

`net8.0` обрано для сумісності з Visual Studio 2022. Це не впливає на логіку лабораторного завдання.

## 3. Відокремлення вимог викладача від нашого оформлення

Початкові вимоги зафіксовані в [ASSIGNMENT.md](ASSIGNMENT.md).

Обов’язковими є чотири алгоритмічні завдання. Заставка, головне меню, класи, повернення до меню та додаткова перевірка введення — це наше оформлення спільного проєкту.

Це важливо, щоб під час покращення програми не змінити зміст завдання.

## 4. Користувацький сценарій

1. Запуск програми.
2. Коротка заставка.
3. Натискання Enter.
4. Головне меню:
   - 1 — перевірка року на високосність;
   - 2 — побудова трикутника;
   - 3 — гра «Хто хоче стати мільйонером?»;
   - 4 — калькулятор;
   - 0 — вихід.
5. Виконання обраного завдання.
6. Пауза та повернення до головного меню.
7. Завершення програми через пункт 0.

Некоректний пункт меню не повинен завершувати програму.

## 5. Архітектура

### Program.cs
Точка входу. Відповідає за:
- налаштування консолі;
- заставку;
- створення доступних завдань;
- цикл головного меню;
- вибір модуля;
- коректний вихід.

### IConsoleTask
Мінімальний спільний контракт:
- `Title` — назва завдання;
- `Run()` — запуск завдання.

Це демонструє базове застосування поліморфізму без складної архітектури.

### LeapYearTask
Відповідає лише за введення року, перевірку введення та визначення високосності.

### TriangleTask
Відповідає за введення висоти та побудову числового трикутника за допомогою циклів.

### MillionaireTask
Керує п’ятьма питаннями, перевіряє всі відповіді, нараховує бали та показує підсумок після п’ятого питання.

### Question
Модель одного питання:
- текст;
- масив із 4 варіантів;
- номер правильної відповіді.

### CalculatorTask
Розбирає арифметичний рядок і обчислює його без `eval` та без `DataTable.Compute`.

### ConsoleHelper
Містить лише повторювані консольні операції:
- заголовок;
- пауза;
- безпечне читання цілого числа;
- повідомлення про помилку.

Алгоритми завдань у цей клас не переносяться.

## 6. Етапи реалізації

### Етап 0 — документація
- README;
- точне вихідне завдання;
- вимоги;
- план;
- тестові сценарії;
- .gitignore.

### Етап 1 — каркас застосунку
- створити solution та .NET console project;
- налаштувати UTF-8;
- реалізувати заставку;
- реалізувати головне меню;
- додати `IConsoleTask`;
- додати чотири модулі-заглушки;
- перевірити неправильний пункт меню.

### Етап 2 — завдання 1: високосність року
Логіка:
1. якщо рік ділиться на 400 без остачі — високосний;
2. інакше, якщо ділиться на 100 — не високосний;
3. інакше, якщо ділиться на 4 — високосний;
4. інакше — не високосний.

Контрольні значення: 1900, 2000, 2024, 2025, 2100.

### Етап 3 — завдання 2: трикутник
Для кожного рядка `i`:
- обчислити відступ;
- вивести число `i` рівно `i` разів;
- перейти на новий рядок.

Штучну верхню межу не вводимо, бо її немає в завданні. Для коректного вигляду при висоті 10 і більше ширину одного елемента потрібно визначати за кількістю цифр у максимальному номері рядка.

### Етап 4 — завдання 3: мільйонер
- рівно 5 питань;
- 4 варіанти у кожного;
- одна правильна відповідь;
- перші два питання відповідають прикладу викладача: прапор України та місто розташування ДТЕУ;
- ще три питання — власні;
- стартовий рахунок 0;
- +100 за кожну правильну відповідь;
- неправильна відповідь дає 0 балів за це питання, але гра продовжується;
- після всіх 5 питань показується підсумковий рахунок;
- усі 5 правильних → 500 балів.

Введення поза 1–4 не рахується неправильною відповіддю — користувач вводить значення повторно.

### Етап 5 — завдання 4: калькулятор
Обов’язкова підтримка:
- `+`;
- `-`;
- `*`;
- `/`;
- пробілів між елементами виразу;
- правильного пріоритету;
- виконання операцій однакового пріоритету зліва направо.

Приклад:
`5 + 5 * 10 + 4 / 2 - 3 = 54`.

Базовий алгоритм:
1. токенізувати рядок на числа та оператори;
2. перевірити правильність послідовності токенів;
3. виконати `*` та `/` зліва направо;
4. виконати `+` та `-` зліва направо;
5. вивести результат.

Обов’язково обробити ділення на нуль і некоректний синтаксис.

Дужки, степені, функції та унарні оператори не входять до вихідного завдання й у першу версію не додаються.

## 7. Стиль коду

- Імена класів і методів — англійською.
- Текст, який бачить користувач, — українською.
- Один метод — одна зрозуміла відповідальність.
- Мінімум дублювання.
- Без складних конструкцій, які важко пояснити на захисті.
- Коментарі лише там, де алгоритм справді потребує пояснення.

## 8. Git-стратегія

Рекомендовані логічні коміти:

- `docs: finalize project specification`
- `feat: add application shell and menu`
- `feat: implement leap year task`
- `feat: implement triangle task`
- `feat: implement millionaire game`
- `feat: implement calculator`
- `test: verify console scenarios`
- `docs: add final run instructions`

## 9. Критерій готовності

Проєкт готовий до здачі, коли:
- усі 4 завдання доступні з одного меню;
- програма не падає на типових неправильних введеннях;
- високосність правильно визначається для років, кратних 4, 100 і 400;
- трикутник має введену висоту;
- гра проходить усі 5 питань незалежно від помилок і правильно рахує підсумкові бали;
- калькулятор дотримується пріоритету й порядку операцій;
- `dotnet build` завершується без помилок;
- проєкт запускається через `dotnet run` та у Visual Studio 2022 17.8+;
- зроблені скриншоти виконання;
- посилання на GitHub готове для здачі.
